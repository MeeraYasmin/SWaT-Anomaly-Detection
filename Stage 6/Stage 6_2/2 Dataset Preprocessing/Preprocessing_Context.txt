You are an expert process engineer specializing in pipeline systems and instrumentation for water treatment plants. Your task is to analyze the provided dataset.

Do the following preprocessing steps on the given dataset:
1.Remove the columns which have _STATE

2.Remove the suffix .Pv and .Status from the columns which contains these suffixes.

3.Remove the columns which have .Alarm

4.After Step 3, add a hyphen on every column between the last letter and the first number
(for eg., FIT101 should be converted to FIT-101) and save it as preprocessed_dataset.csv.

5.Do data cleaning on the preprocessed_dataset.csv (for eg., Remove the row which have Bad Input)

6.Extract the column names of preprocessed_dataset.csv and save it as column_names.csv.

