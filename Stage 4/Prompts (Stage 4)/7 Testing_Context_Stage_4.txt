Invariants:

You are an expert process engineer specializing in pipeline systems and instrumentation for water treatment plants. Your task is to test the stage_4_components.csv for all the 3 invariants in generate_invariants_4.py and store the output as test_results_4.csv.

Rules for the testing:
Include all the columns with their values of stage_4_components.csv without hyphens on the test_results_4.csv.
Save these extracted columns in a xlsx file named test_dataset_4.xlsx.
Create the columns invariant_1,..., invariant_n for the n number of invariants.

Check the invariants with each and every row of the given dataset.
 If they follow the invariants, keep the value of that invariant column as "yes".
 If it didn't follow the invariants, keep the value of that invariant column as "no".

Find the count of "yes" and "no" on all invariants columns.

Create another csv named anomaly_count_4.csv and have 3 columns (invariant_number, anomaly, not anomaly)
The invariant_number denotes the n number of invariants (from invariant_1,..., invariant_n)
The count of "yes" on all invariants should go to not anomaly column.
The count of "no" on all invariants should go to anomaly column.

Create another csv named minimum_windowsize_4.csv and have 2 columns (invariant_number, minimum_windowsize)
The invariant_number denotes the n number of invariants (from invariant_1,..., invariant_n)
The minimum_windowsize is calculated for all the invariants in the check_invariants_4.py program. 

Create another csv named anomalies_4.csv with the following columns:
Plant stage, Alert Code, Antecedent, Consequent, Comments

Here, the Plant stage is always 4
Alert Code is A2-number (where number is the number of the invariant (for eg., A2-01,...))
Antecedent is the if condition of the anomaly_statement_4.txt
Consequent is the then condition of the anomaly_statement_4.txt

Ensure Antecedent and Consequent have component names without hyphens.
(for eg., Antecedent: P401=2, Consequent: FIT401>Tmin4)

Comments should show the connection between the antecedent and consequent of the anomaly_statement_4.txt
(for eg., P401 RUN and no flow on FIT401 implies anomaly)

Rules for anomaly_statement_4.txt:
1. For each invariant, identify the maximum number of consecutive rows where the invariant is violated (Antecedent is True, but Consequent is False).
2. Set minimum_windowsize = (max_consecutive_violations + 1).
3. Generate a statement in this format: 
   INV#: If [Antecedent], after a window of [minimum_windowsize] samples, if [Negated_Consequent] then it is an anomaly.
4. Use the Negated_Consequent to describe the failure condition (e.g., if Consequent is FIT401>Tmin4, Negated is FIT401<=Tmin4).
5. Ensure Antecedent and Negated_Consequent have component names without hyphens.

